# overlays/ — targeted patches to upstream files

This directory contains patches applied on top of upstream Documenso files. **Every patch must have a header explaining intent + fragility estimate.** See `UPSTREAM.md` for conventions.

## Active patches

| # | Patch | Status | Fragility | Why we need it |
|---|---|---|---|---|
| 076 | `076-recipient-token-authorization.patch` | **APPLIED 2026-09-12 (branch; deployment pending)** | HIGH | A-05: owned recipient access policy gates legacy/v2 fields, completion/rejection, CSC entry, item/attachment/multisign/status reads and all three PDF adapters. Account-backed signing factors must belong to the recipient; ACCOUNT and completion 2FA are cumulative; FREE_SIGNATURE gets ACTION auth and the matching UI prompt. Draft and deleted unfinished document reads are denied; recipients retain finalized copies after sender deletion. Enabled direct-template PDF previews retain their separate login contract. PDFs use private/no-store, and signing deadlines leave authorized downloads available. Policy is in `packages/bizrethink/server-only/recipient-access.ts`; the patch contains hooks only. Recheck every token consumer on upstream sync. Tests: three owned recipient regression suites plus the exact owned Playwright API spec. |
| 001 | `001-add-bizrethink-claim-tier.patch` | **APPLIED 2026-04-29** | MEDIUM | Adds a 7th `BIZRETHINK` tier to `INTERNAL_CLAIM_ID` + `internalClaims` in `packages/lib/types/subscription.ts`. Mirrors ENTERPRISE flags; HIPAA + allowLegacyEnvelopes intentionally omitted. Pairs with overlay 002. |
| 002 | `002-route-org-creation-to-bizrethink-tier.patch` | **APPLIED 2026-04-29** | LOW | Repoints all three org-creation call sites (`createPersonalOrganisation`, `createOrganisationRoute`, `createAdminOrganisationRoute`) from `INTERNAL_CLAIM_ID.FREE` to `INTERNAL_CLAIM_ID.BIZRETHINK`. Single-token swaps; admin route also gains an import. |
| 003 | `003-build-heap-size.patch` | **APPLIED 2026-04-30, re-affirmed 2026-09-07** | MEDIUM | Adds `ENV NODE_OPTIONS="--max-old-space-size=4096"` in `docker/Dockerfile` before the turbo build step. Documenso's tsc + react-router build OOMs at Node's default ~1GB heap. Build container needs ≥4GB available RAM. | **2026-09-07 sync: upstream #3332 raised this to 8192 and we deliberately did NOT follow.** The Coolify host that builds the image has 8GB TOTAL; letting V8 grow to 8GB leaves nothing for the OS and re-creates the silent build OOM a 4GB VPS already produced. Build peaks ~4GB. If the Docker build dies on heap, raise incrementally -- never to 8192 on an 8GB box. Fragility LOW->MEDIUM: upstream now writes this same ENV, so every future sync conflicts here.
| 004 | `004-add-bizrethink-to-claims-description.patch` | **APPLIED 2026-04-30** | LOW | Adds `[INTERNAL_CLAIM_ID.BIZRETHINK]: ''` to the typed `internalClaimsDescription` map in `apps/remix/app/components/dialogs/organisation-create-dialog.tsx`. Required because overlay 001 added BIZRETHINK to the enum, and this map is typed as `{ [key in INTERNAL_CLAIM_ID]: ... }` so tsc requires every enum key. Empty description is fine — BIZRETHINK is never user-selectable in the billing UI. |
| 005 | `005-suppress-license-banner-self-host.patch` | **APPLIED 2026-04-30** | LOW | Replaces the body of `apps/remix/app/components/general/admin-license-status-banner.tsx` with `return null`. The licence server returns UNAUTHORIZED for our BIZRETHINK tier (overlay 001 has EE-flagged features), but self-host has no functional EE gate per the paywall audit. First attempt added `UNAUTHORIZED` to the early-return guard but broke `tsc` (downstream code referenced UNAUTHORIZED, type narrowed to "no overlap"); current version replaces the whole component with a stub. Caller at `root.tsx` still passes the `license` prop — kept the prop name + export shape, retyped to `unknown` to drop the upstream import. |
| 006 | `006-prune-upstream-only-workflows.patch` | **APPLIED 2026-04-30 (amended same day to add codeql-analysis.yml)** | MEDIUM | Deletes 13 GitHub Actions workflow files (Crowdin translations, DockerHub publish, Documenso community labelers, stale bot, tag-based deploy, advanced CodeQL config) + 1 orphaned labeler config. They have no purpose on a self-host fork and were generating failure-email noise on every push. The advanced CodeQL workflow specifically collided with the repo's default CodeQL setup ("CodeQL analyses from advanced configurations cannot be processed when the default setup is enabled"); deleting the advanced workflow keeps the default setup as the active scanner. Adds a "Re-prune upstream-only workflow files" step to `upstream-sync.yml` so weekly merges that re-introduce these paths auto-delete them and commit before the sync PR opens. Conflicts case (modify/delete) intentionally left for human review. |
| 007 | `007-prisma-merge-additions.patch` | **APPLIED 2026-04-30** | LOW | Wires `packages/bizrethink/prisma-extensions/merge-into-schema.mjs` into `packages/prisma/package.json`'s build/prebuild/post-install/prisma:generate/prisma:migrate-* scripts. The merge script reads `additions.prisma` and appends its content into `schema.prisma` between marker comments (idempotent). Required prerequisite for Phase B (overlay 009 — per-org SMTP) and Phase C (overlay 010 — instance signing UI), which both add Prisma models. Patch surface is one new script key + six modified script prefixes. |
| 008 | `008-unblock-email-domains-self-host.patch` | **APPLIED 2026-04-30 (extended TWICE same day — UI then backend)** | LOW | Removes self-host paywall gates from SEVEN files (3 frontend + 4 backend). Frontend: `o.$orgUrl.settings.email-domains._index.tsx`, `.$id.tsx`, and `_layout.tsx` (page-render + nav-link visibility for both Email Domains and Authentication Portal). Backend: `create/delete/verify-organisation-email-domain.ts` and `update-organisation-authentication-portal.ts` (TRPC mutations were throwing "Billing is not enabled" before reaching the downstream claim-flag check). All seven follow the same pattern: remove the redundant `IS_BILLING_ENABLED` gate, let the downstream claim-flag + organisation-membership checks do the actual authorization. Audit found 13 frontend + 8 backend references; the other 14 correctly handle self-host. **Lesson learned:** when fixing a paywall feature, grep BOTH `apps/remix` AND `packages/trpc/server` for `IS_BILLING_ENABLED` — frontend gates show blank UI; backend gates throw on submit. |
| 009 | `009-replace-ses-with-local-dns.patch` | **APPLIED 2026-04-30** | MEDIUM | Replaces Documenso's AWS SES dependency in the email-domain feature with a local `dns.resolveTxt` lookup. Documenso was using SES purely as a domain-verification middleman (CreateEmailIdentityCommand on add, GetEmailIdentityCommand on verify); we don't send through SES (Postmark handles all sending). Same domain-ownership security guarantee — DNS resolution is the actual proof of zone control. Patches `packages/ee/server-only/lib/create-email-domain.ts` (removes SES register call) and `packages/ee/server-only/lib/verify-email-domain.ts` (replaces SES poll with `dns.resolveTxt` on the selector FQDN, byte-compares the `p=` value to our stored public key). Net effect: no AWS dependency, no SES env vars needed, identical security. The `getSesClient` and `verifyDomainWithDKIM` exports remain in the file as dead code — they'll throw "Missing AWS SES credentials" if some future caller imports them, which is fine because nothing in this build does. |
| 011a | `011a-instance-signing-config.patch` | **APPLIED 2026-05-01, regenerated 2026-09-07** | HIGH | DB-first signing config in `packages/signing/index.ts`: transport, TSA URLs and PAdES contact info read the `BizrethinkInstanceSigningConfig` singleton before falling back to env, so certs rotate from `/admin/signing` without a redeploy. **Cryptographically load-bearing** -- this selects the key every sealed PDF is signed with. 2026-09-07: upstream #3309 rewrote `getCertificateStatus` to inspect the signer and call `createLocalSigner({ buildChain: false })`, so `createLocalSigner` now accepts upstream's options object while keeping DB-first cert/passphrase resolution; the env fallback defers to `NEXT_PRIVATE_SIGNING_TRANSPORT()`, which upstream gave the same `|| 'local'` default. Consequence: `/api/health` and `/api/certificate-status` now report on the DB-sourced cert rather than env alone. This row did not exist before 2026-09-07 -- the index has other gaps. |
| 034 | `034-signature-width-height-meta.patch` | **APPLIED 2026-05-08** | LOW | Adds `width=` / `height=` placeholder params to ZSignatureFieldMeta + override at `auto-place-fields.ts` bbox writer. Lets `{{SIGNATURE, r1, width=240, height=30}}` produce wider widgets than the placeholder text would otherwise dictate — fixes signature-clipping ("Test Mercha" instead of "Test Merchant") on tight templates. Bounds 40-600pt × 20-200pt. |
| 035 | `035-date-fontweight-meta.patch` | **APPLIED 2026-05-08** | LOW | Adds `fontWeight: 'normal' \| 'bold'` to ZDateFieldMeta + applies via Konva fontStyle in render-generic-text-field. Lets `{{DATE, r1, fontSize=10, fontWeight=bold}}` render bold dates so executed-on dates pop visually. Other generic-text types (TEXT/NAME/EMAIL/NUMBER) ignore the meta key. |
| 036 | `036-per-page-verification-footer.patch` | **APPLIED 2026-05-08** | LOW | At seal time (after rejection-stamp pass, before certificate copyPagesFrom), draws a small Dotloop-style verification footer at the bottom of every contract-body page: `Verified by sign.pacta.ink · Envelope <id> · Signed <ts> UTC`. Footer-drawing helper lives at `packages/bizrethink/server-only/pdf/add-verification-footer-to-pdf.ts` (additive, no patch). Skips rejected docs (which already carry the diagonal "DOCUMENT REJECTED" stamp). Survives single-page screenshots; visible in any PDF viewer regardless of PAdES validation support. |
| 037 | `037-inline-signature-badge.patch` | **APPLIED 2026-05-08** | MEDIUM | In render-signature-field's `mode === 'export'` branch, draws a small `Signed YYYY-MM-DD HH:MM UTC` badge (Inter 6.5pt gray-500) directly below each signature glyph. Tier 2 reinforcement complementing 036's page footer. Threads `signature.created` through both copies of FieldToRender (the duplicate type in `field-renderer.ts` and `render-field.ts` — kept in sync). Only renders when the signature has a signed-on timestamp. |
| 038 | `038-compact-audit-log-rows.patch` | **APPLIED 2026-05-08** | MEDIUM | Replaces upstream's bordered-card audit-log row layout with a 2-line compact format: line 1 = `[dot] EVENT_TYPE — Description … timestamp`; line 2 = `email · ip · OS · browser`. All upstream fields preserved (UA decomposed for compactness). Result: 30-event envelope fits in ~2-3 pages instead of 8-10. If upstream restyles the audit log, re-derive the compact layout from the new shape. |
| 043 | `043-add-pro-business-tiers-and-ai-flag.patch` | **APPLIED 2026-05-10** | MEDIUM | Adds PRO + BUSINESS public SaaS tiers to `INTERNAL_CLAIM_ID` + `internalClaims` in `packages/lib/types/subscription.ts`, and a new `aiEnabled` ClaimFlag (with `SUBSCRIPTION_CLAIM_FEATURE_FLAGS` metadata) gating AI-assisted document creation. Pro: 100 docs/mo, 5 members, branded signing + AI ($35/mo, $350/yr). Business: unlimited docs, 10 members, full compliance flags + AI ($199/mo, $1990/yr). Also extends `internalClaimsDescription` in `apps/remix/app/components/dialogs/organisation-create-dialog.tsx` with PRO + BUSINESS strings (typed exhaustive map requires entries for every enum key — same pattern as overlay 004 for BIZRETHINK). Pairs with overlay 041 (routes new external orgs to PRO+trial) and Stripe Products w/ `claimId` metadata. Fragility: MEDIUM — upstream tier templates and the enum are the conflict surface for future merges. |
| 041 | `041-route-new-orgs-to-pro-trial.patch` | **APPLIED 2026-05-10** | MEDIUM | **Supersedes overlay 002.** Revises the three org-creation call sites (`createPersonalOrganisation`, `createOrganisationRoute`, `createAdminOrganisationRoute`) to assign PRO claim with a 14-day trial instead of BIZRETHINK. New external orgs go through trial → FREE downgrade flow (trial-expire-sweep cron in Phase 2 of SaaS plan handles downgrade). Each call site also invokes the additive helper `@bizrethink/customizations/server-only/billing/start-trial-for-new-org` to record `bizrethinkInternal=false` + `trialStartedAt`/`trialEndsAt` in the new `BizrethinkOrganisationBilling` table (added in migration 20260511015534). Existing 8 production orgs retroactively flagged `bizrethinkInternal=true` via `packages/bizrethink/migrations/2026-05-10-stamp-internal-orgs.sql` (run-once data migration). Fragility: MEDIUM — same as overlay 002 (every new upstream org-creation path needs adapting). |
| 053 | (inline edit) | **APPLIED 2026-05-11** | LOW | Filter system service accounts out of `/admin/users` table. Modifies `findUsers` in `packages/lib/server-only/user/get-all-users.ts` to add `email: { notIn: [deletedServiceAccountEmail(), legacyServiceAccountEmail()] }` to the where clause. Reason: `deleted-account@sign.bizrethink.ai` + `serviceaccount@sign.bizrethink.ai` are load-bearing system rows that shouldn't appear as users an admin might edit/delete. Filter at query level so pagination stays accurate. Fragility: LOW — single AND-wrap of the existing OR clause. |
| 060 | (inline edit, six files) | **APPLIED 2026-05-12** | LOW | Repoint all in-app `docs.documenso.com` links to `pacta.ink/docs` after standing up Pacta-specific helper docs on the marketing site (see `~/.claude/plans/harmonic-squishing-lecun.md`). Six files touched: `o.$orgUrl.support.tsx` (Documentation card on the support page), `t.$teamUrl+/settings.tokens.tsx` (API tokens explainer), `subscription-claim-form.tsx` + `admin-license-card.tsx` + `admin+/organisations.$id.tsx` (three "Learn more" links on enterprise/license panels — now point at the Choose-a-Plan article), `team-member-create-dialog.tsx` (member-management explainer — now points at Organisation Setup). Plus overlay 059 (CI exclude `apps/docs` from `npm run build`, already shipped). Fragility: LOW — single string replacement per file, no logic changes. |
| 054 | (inline edit) | **APPLIED 2026-05-11** | LOW | Pacta-branded signup hero replacing Documenso's `UserProfileTimur` (Timur Ercan, Documenso founder). New `apps/remix/app/components/general/user-profile-pacta.tsx` mirrors the layout but uses a generic B2B persona ("Acme Capital") with initials-avatar tile (no image asset needed). Also rewrites two strings in `apps/remix/app/components/forms/signup.tsx`: floating pill ("User profiles are here!" → "Built for agreements that hold.") and hero subhead ("Open and beautiful signing is within your grasp." → Pacta-voice "Send, sign, and seal contracts with timestamped audit trails and AATL-grade cryptographic signatures."). Background community-cards.png left alone — replacing it needs a design asset. Signin page didn't have the Timur hero so no signin change needed. Fragility: LOW — additive new component + two trans-string swaps. |
| 051 | (inline edit) | **APPLIED 2026-05-11** | LOW | Swap 5 billing-router files from sync `IS_BILLING_ENABLED()` (env-var) to async `isBillingEnabledFromConfig()` (DB-aware). Files: `packages/trpc/server/enterprise-router/{get,create,manage}-subscription.ts`, `get-invoices.ts`, `get-plans.ts`. Without this, every in-app billing TRPC call hangs/errors because Pacta deliberately doesn't set `NEXT_PUBLIC_FEATURE_BILLING_ENABLED` — billing is DB-configured via /admin/stripe (overlay 045). Other call sites of `IS_BILLING_ENABLED` in email-domain routes already covered by overlay 008 / overlay 014. Fragility: LOW (one-line swaps; the async getter has been in place since overlay 045c). |
| 050 | (inline edit) | **APPLIED 2026-05-11** | LOW | Drop the `IS_BILLING_ENABLED()` filter on the Billing nav entry in `apps/remix/app/routes/_authenticated+/o.$orgUrl.settings._layout.tsx`. Upstream's gate reads the `NEXT_PUBLIC_FEATURE_BILLING_ENABLED` env var, but Pacta configures billing via /admin/stripe (DB-backed, overlay 045) and deliberately leaves the env var unset. Without this overlay, the Billing nav entry stays hidden in every org settings sidebar — Personal Org users had no path to upgrade. The billing page itself doesn't gate on the same flag, so always showing the nav entry is the right behavior. Original filter preserved as a comment for easy revert if needed. Fragility: LOW. |
| 049 | (inline edit) | **APPLIED 2026-05-11** | LOW | Always render `<OrgMenuSwitcher />` in `apps/remix/app/components/general/app-header.tsx:86`, replacing the upstream `isPersonalLayout()` conditional that swapped in a stripped `<MenuSwitcher />` for solo-Personal-Org users. Reason: upstream's MenuSwitcher has NO link to `/o/<orgUrl>/settings`, making the Personal Org's billing page unreachable from the UI. For Pacta SaaS this is a blocker — Personal Org users ARE paying customers (Free → Pro → Business) and need to reach billing. Other 9 `isPersonalLayout` call sites left alone (those control adjacent UX simplifications that are still fine). MenuSwitcher.tsx is now dead code but left in place to keep the upstream diff surface small. Fragility: LOW (single-line conditional removal + unused-import cleanup). |
| 048 | (inline edit) | **APPLIED 2026-05-11** | MEDIUM | Phase L: auto-claim pending invites on signup + skip Personal Org. Touches `packages/lib/server-only/user/create-user.ts` (onCreateUserHook now calls `autoClaimInvitesOnSignup` first; only creates Personal Org if no invites were claimed). Closes a major UX gap where domain-gated signups landed in a blank Personal Org despite having a pending invitation. Pair: 048b (require-invite-when-gated guard in `packages/auth/server/routes/email-password.ts` POST /signup) + 048c (signup-form `<PendingInvitePreview>` inline component in `apps/remix/app/components/forms/signup.tsx`). Schema extension to `ZSiteSettingsSignupSchema` (added `requireInviteWhenDomainGated`) + admin Site Settings UI toggle + `bizrethink.signupInvite.lookup` public TRPC procedure. Fragility: MEDIUM — auth signup flow is a hot path for upstream merges; revalidate after every upstream sync. **Amended by 071 (2026-09-10):** invites are now claimed only for a VERIFIED email (in `verifyEmail`, not at signup), and 048c's `<PendingInvitePreview>` and the public `bizrethink.signupInvite.lookup` it called are **removed** — the lookup told anyone which orgs had invited any email. |
| 047 | (inline edit) | **APPLIED 2026-05-11 (extended same day for sidebar reorg)** | MEDIUM | Reorganizes the admin sidebar in `apps/remix/app/routes/_authenticated+/admin+/_layout.tsx`. Replaces 14 hand-rolled flat `<Button>` nav rows with a data-driven `NAV_GROUPS` array rendered through internal `NavGroup` + `NavItem` helpers. Four section headers: **Insights**, **Data**, **Identity & Access**, **Configuration**. Items added by BizRethink overlays carry a small `BR` `Badge` (variant `secondary`) — including Email Domains (per user 2026-05-11: SES→DNS swap + paywall overlay qualify it as BR even though page UI is upstream). Three disabled "Soon" placeholders reserve sidebar space for planned pages (Audit Log, Jobs & Crons, API Keys & Webhooks). First inline edit also added the "Billing & Stripe" link to the new `/admin/stripe` page (Phase 1h of Stripe-UI). Fragility: MEDIUM — upstream may restyle `_layout.tsx` or add new admin routes; re-apply by re-deriving entries in `NAV_GROUPS` (single source of truth). Pattern reduces future merge surface (one array vs 14 button blocks). | **Extended 2026-09-07:** the sidebar entry for the clause library reads **Lease Clauses**, not "Clause Library". An MCA vertical is being built with a library of its own, so the generic name is ambiguous at the one place a person chooses between them; the distinguishing noun goes first so a future "MCA Clauses" reads as its pair. Label text only — no nav structure change, so `NAV_GROUPS` remains the single source of truth and the re-derivation instruction above is unchanged. **Extended 2026-09-07 (ADR 0008):** an **MCA Conformity** entry under **Data** (`/admin/mca`, `ScaleIcon`, `BR`), a second entry beside Lease Clauses rather than a tab inside it. **Extended 2026-09-08:** an **MCA Clauses** entry under **Data** (`/admin/mca-library`, `ScrollTextIcon`, `BR`), completing the pair the 2026-09-07 rename anticipated — the clause library is the MCA vertical's *other* surface and needs an approval workflow that MCA Conformity must never acquire, so it is a separate entry for the same ADR 0008 reason. Structure change only where the array gains a row; `NAV_GROUPS` remains the single source of truth. The MCA conformity surface is read-only and has no approval workflow — approving a regulator's prescribed words would record an attorney's name against California's text — and sharing a page with the library that does have one is how it would acquire one. Structure change only where the array gains a row; `NAV_GROUPS` remains the single source of truth.
| 044 | `044-pacta-billing-banner.patch` | **APPLIED 2026-05-10** | LOW | Adds `PactaBillingBanner` component (inline, ~50 lines) at the top of `apps/remix/app/routes/_authenticated+/o.$orgUrl.settings.billing.tsx`. Reads from new `trpc.bizrethink.organisationBilling.get` procedure (additive in `packages/bizrethink/server-only/trpc/organisation-billing-router.ts`) and renders one of four states: BizRethink-internal badge (blue), Pro-trial-active (indigo, or amber if ≤3 days remaining), trial-ended (red with upgrade CTA), or silent fallback. Inline definition vs `packages/bizrethink/ui/` because adding React + UI deps to bizrethink package is overkill for a single banner — refactor when more BizRethink in-app UI components arrive. Fragility: LOW (new lines added at the top, one JSX insertion in the return value). |

| 066 | `066-e2e-testignore-fork-divergence.patch` | **APPLIED 2026-08-13** | MEDIUM | One-time fork-test curation. `testIgnore` (whole-file) for `form-flattening.spec.ts` (overlays 018/040 — fork keeps AcroForm widgets un-flattened) and `envelope-alignment/overflow.spec.ts` (overlay 061 — pixel-diff vs WarpBuild baselines on ubuntu-latest); `test.skip` on `webhooks-crud` create/update (overlay 024 — dialog copy rebranded Documenso→Pacta breaks the locator; view/delete keep running). Backed by a full-inventory diagnostic (8 failed/6 flaky/812 passed) + 13-agent adversarial categorization that found **0 real bugs**. Flakes left in (accept-on-retry). Backfilled by `packages/bizrethink/regression-tests/acroform-flatten-skip.test.ts`. Policy: `FORK-TESTING.md`. Fragility: MEDIUM (spec paths + webhook test titles are the conflict surface). |
| 067 | `067-e2e-max-failures.patch` | **APPLIED 2026-09-02** | LOW | `maxFailures` 1 -> 25 in the CI branch. Upstream aborts the run on the first failure, so with `retries: 4` a flaky spec exhausts five attempts and then ends the suite: PR #67 reported *4 failed / 6 interrupted / **888 did not run** / 183 passed* for a change entirely inside `packages/bizrethink/lease/` with no lease spec failing. Three occurrences in one day, each costing a ~25-minute re-run. 25 still fails fast on a broken build while letting an isolated flake report beside the tests that passed. Does NOT fix the underlying `find-documents` team-visibility flake — see `FORK-TESTING.md`. Fragility: LOW (one config field). |
| 068 | `068-seed-documents-await.patch` | **APPLIED 2026-09-06** | LOW | Terminates the `match()` chain in `seedDocuments` (`packages/prisma/seed/documents.ts`) with `.otherwise(async () => null)`. Without a terminator ts-pattern returns a `Match` object rather than the handler's Promise, so the enclosing `Promise.all` awaits nothing and the seeding is **fire-and-forget** — specs query the API while rows are still being written and receive an arbitrary subset. **This is the `find-documents` Team Context flake** (~1 CI run in 5 for months, and the thing overlay 067 was written to make readable but explicitly did not fix). Four earlier root causes were wrong: DB contention on the document counter, cross-suite interference, accumulated DB state, and a silent role downgrade. The tell was a continuous `Received:` distribution of 0,1,2,3,4,5 across 20 repeats — no filter or role rule produces that, only a partial write. Intermittent because it races the unawaited seeds (~19-38ms) against the gap before the query (~6-12ms + HTTP), which is why an **idle** box fails *more* and a freshly reset DB failed *more*, not less. A/B same box: unpatched 20 failed/0 passed, patched 0 failed/20 passed; full file 9 failed/91 passed → 100 passed. 49 call sites across 5 spec files were all racy. Guarded by `packages/bizrethink/regression-tests/seed-documents-await.test.ts`. Worth reporting upstream — it is a real documenso bug, not fork divergence. Fragility: LOW.
| 069 | `069-leases-nav-entry.patch` | **APPLIED 2026-09-07** | LOW | Adds a **Leases** entry to the team-level main nav, in both `apps/remix/app/components/general/app-nav-desktop.tsx` and `app-nav-mobile.tsx` — the lease builder shipped at `/t/:teamUrl/leases` with no link to it from anywhere in the product, reachable only by typing the URL. Both files or nothing: an entry in one is a feature that is invisible on the other. No extractable definition to patch instead — upstream moved the SETTINGS nav to `packages/lib/utils/settings-nav.ts` in the 2026-08-13 sync but left the app nav as an array literal inside each component, with a different item shape in each (`label`+`msg` vs `text`+`t`). The insertion is therefore one spread expression appended to an array upstream already maintains as data, plus an import and a hook call — the shape overlay 047 had to *create* for the admin sidebar already exists here, so this only adds a row. The gate is the substance: `canAccessLeaseBuilder` is server-only and the nav renders client-side, so the flag crosses as a READ of the gate (`bizrethink.leaseBuilder.access` → `listLeaseBuilderOrganisationIds` → the same `canAccessLeaseBuilder` every lease loader calls) rather than a copy of the rule, mapped onto the session's team tree by the additive hook at `apps/remix/app/components/general/lease/use-lease-builder-team-urls.tsx`. A nav entry that can disagree with the route's own 404 is worse than no nav entry, and this one cannot. Rejected: the organisation session (patches upstream's session query *and* its zod schema, and adds two grant lookups per organisation to a query that re-runs on every navigation and on window focus) and a route loader boolean (the nav is rendered by `_authenticated+/_layout.tsx`, which sets `shouldRevalidate = () => false`, so it would describe whichever team was landed on first). Guarded by `packages/bizrethink/regression-tests/leases-nav-entry.test.ts`. No `BR` badge — that marker is for `/admin` staff pages (overlay 047); Leases is a customer feature and sits beside Documents and Templates. Fragility: LOW — one appended line per file; if upstream restyles the array, re-append it, there is no logic to re-derive. |
| 071 | `071-signup-hardening.patch` | **APPLIED 2026-09-10** | MEDIUM | Response to the 2026-09-10 external probe (signup had been open since launch; see `docs/state/inflight/fix-signup-hardening.md`). The logic lives in `packages/bizrethink/`; the upstream edits are the hooks it needs. **(a) Invites claimed only for a verified email:** `packages/lib/server-only/user/create-user.ts` (`onCreateUserHook` claims only when `emailVerified` is set, and defers the Personal Org for an unverified user with a pending invite) and `packages/lib/server-only/user/verify-email.ts` (one call to `claimInvitesOnVerification` after VERIFIED). **(b) SSO removed from the build:** the org-portal options getter throws, and the org-portal sign-in loader returns 404 (Google/Microsoft/OIDC are killed in BizRethink's `getProviderConfig`, no upstream edit). **(c) Signup fails closed:** the admin signup-gating section shows "closed" with no saved row; `packages/prisma/seed/bizrethink.ts` re-seeds an open row on reset instead of deleting it (the suite is `fullyParallel` on one DB); `packages/app-tests/e2e/user/auth-flow.spec.ts` opens signup before its signup test. **(d) Shrinks overlay 048c:** `PendingInvitePreview` is removed from `apps/remix/app/components/forms/signup.tsx` with the public `signupInvite.lookup` it called. Plus a comment fix in `packages/auth/server/routes/email-password.ts` (048b). Guarded by `signup-config.test.ts`, `schemas/signup.test.ts`, `auto-claim-invites-on-signup.test.ts`, `create-user.test.ts`, `verify-email.test.ts`, `regression-tests/sso-kill-switch.test.ts`, `regression-tests/signup-invite-lookup-removed.test.ts`. Fragility: MEDIUM — `create-user.ts` and `verify-email.ts` sit on upstream's auth hot path (as 048 already does); every other edit is a guard line or a comment. |
| 073 | `073-admin-loader-authz.patch` | **APPLIED 2026-09-11** | MEDIUM | Closes A-01 from the 2026-09-10 security audit — an **anonymous** authorization bypass. Documenso's admin authorization lives only in the admin layout loader (`admin+/_layout.tsx`); under React Router 7 single-fetch, `/admin/<page>.data?_routes=routes/_authenticated+/admin+/<page>` runs that leaf loader alone and skips every ancestor, so the layout check never runs. Eight upstream admin leaf loaders (`claims`, `documents.$id`, `organisation-insights._index`, `organisation-insights.$id`, `organisations.$id`, `site-settings`, `stats`, `users._index`) were reachable unauthenticated — exposing documents with recipients/fields/signatures/**signing tokens**, user emails/roles, org insights and the CAPTCHA secret across `mfg`/`lombard`. Each now calls `requireAdminLoader(request)` (new owned file `packages/bizrethink/server-only/require-admin-loader.ts`, `getOptionalSession` + `isAdmin` → 404), the same idiom the fork's own admin pages (`mca`, `mca-library`, `lease-library`) already used. Guarded by `regression-tests/admin-loaders-gated.test.ts` (scans the admin dir; fails any ungated leaf loader — it already caught `organisations.$id`, which the audit's list of 7 missed) and `server-only/require-admin-loader.test.ts`. `@documenso/auth` added to the bizrethink package deps. Fragility: MEDIUM — upstream owns these route files and single-fetch is framework behaviour; a sync that adds an admin page or reformats these conflicts here, but the scan test makes the failure loud. Follow-up: request-level middleware as defence in depth (audit's A-01 §). |
| 075 | `075-api-token-team-boundary.patch` | **APPLIED to branch 2026-09-12** | HIGH | A-02: retain the authenticated API token's issuing team through v1/v2 handlers and the three Hono PDF download paths that bypass tRPC. Owned `server-only/api-token-team-scope.ts` uses `AsyncLocalStorage.run` with a global singleton across bundle copies; single/bulk envelope predicates add its team outside the ownership/team-email OR. The preliminary document-delete lookup also applies it so recipient self-hide cannot modify a foreign team. Human-session ownership semantics, same-team visibility and token lifecycle/rate limits remain intact. Regressions exercise real handlers with DB doubles; the owned Playwright API spec covers HTTP/PostgreSQL. Keep 074 reserved for R-01 #169. Fresh adversarial review and two reviewers for the common query predicates are required.  CI follow-up: two upstream unauthorized-delete E2Es assert the intentional 404 and verify no document deletion. |
| 072 | `072-dependency-security-floors.patch` | **APPLIED 2026-09-10** | MEDIUM | Patches the open Dependabot advisories (32 alerts; prod `npm audit` 20 → 6, 0 critical — the 6 left are two ACCEPTED chains). Raises declared versions in upstream `package.json` files (`nodemailer` ^9.1.1, `sharp` 0.35.4, `csv-parse` ^7.0.2, `colord` ^2.9.4, `@simplewebauthn/server` ^13.3.2, `@faker-js/faker` ^10.5.0, `vitest` ^4.1.11, and `next` 16.3.3 in `apps/docs` + `apps/openpage-api`) and extends the root `overrides` block (`hono` ^4.13.5, plus `qs`, `morgan`, `joi`, `@shuding/opentype.js → fflate`). **Durability is the point:** the sync takes upstream's lockfile wholesale, so a lockfile-only fix would be reverted weekly; raised ranges make `npm install` re-resolve direct deps, and — because npm does NOT apply an override to a package already in the lockfile — `UPSTREAM.md` now runs `npm update qs morgan joi fflate` after `npm install` (simulated: 4 floors regress without it, none with it). Guarded by `packages/bizrethink/regression-tests/dependency-security-floors.test.ts`, which reads the root lockfile and also holds the ACCEPTED register (`deepmerge-ts`, `ts-deepmerge`, `adm-zip`). `package.json` cannot carry a `MODIFIED` comment, hence this row. Fragility: MEDIUM — every file is one upstream bumps too; on conflict keep the HIGHER version, and drop an override once upstream's own range reaches the floor (the test keeps the floor). |

| 074 | `074-signup-policy-snapshot.patch` | **APPLIED 2026-09-11** | MEDIUM | Closes audit R-01: email signup reads one validated `site.signup` policy and retains its disabled/domain/invitation rules throughout the request. Independent later reads could previously lose restrictions on failure. Policy logic and HTTP regression tests live in `packages/bizrethink/`; this overlay wires the upstream handler and updates its source guards. Missing, disabled, invalid or unreadable policy stays closed; legacy getters reject instead of returning permissive defaults. Existing valid-policy env domain fallback and DB-domain invitation semantics are preserved. No cross-request cache or deployment/configuration change. |

| 077 | `077-template-pdf-source-authorization.patch` | **APPLIED to branch 2026-09-12** | HIGH | A-04: resolve every replacement PDF before copying any bytes. The template helper delegates to owned source authorization; browser/presign upload and multipart envelope use record authenticated ownership for fresh staged PDFs. Existing attachments require current envelope permissions, visibility and nondeleted state; API keys retain their issuing-team boundary. New `BizrethinkPdfUpload` is generated from the owned Prisma extension with an additive migration. Unowned historical orphan uploads require re-upload; original authorized template PDFs keep working. Stored fingerprints prevent reusing receipts after reference/content changes, and receipts never override attachment permissions. Recheck all template/upload entry points on sync. Auth/schema/upstream changes require fresh independent adversarial review. A-03 and A-06 remain separate. |

> **Note on the original `001-default-claim-enterprise.patch`:** That patch was written against an older shape of `create-organisation.ts` (had a default-fallback). By the time of the day-1 spike, upstream had refactored to require `claim` as a parameter at every call site, breaking that single-line strategy. The patch was deleted and replaced by 001+002 above. See `~/.claude/projects/-Users-shwet-github-bizrethink-internal-bizrethink-pacta-platform/memory/documenso_paywall_audit.md` for the staleness note.

## Relocations from the 2026-08-13 upstream sync (142 commits, upstream 2.16.0)

Upstream refactored several files out from under these overlays. The behavior was
re-applied at the new anchor points — **the table above still names the old file for
some of these, so check here first when re-deriving a patch.**

| Overlay | Old anchor (gone) | New home |
|---|---|---|
| 008 (nav gating), 050 (Billing entry), 010 (SMTP nav item) | `o.$orgUrl.settings._layout.tsx` nav array | `packages/lib/utils/settings-nav.ts` → `getSettingsNavGroups` |
| 052 (dahlia `current_period_end` fallback) | `webhook/on-subscription-created.ts` + `-updated.ts` (deleted upstream) | `packages/ee/server-only/stripe/sync-stripe-customer-subscription.ts` |
| 013 (DB-first storage config) | `universal/upload/server-actions.ts` (now a delegation shim) | `universal/upload/providers/resolve-storage-settings.ts` + `providers/s3-provider.ts`; `providers/index.ts` no longer throws on an unset transport env |
| 021 #3b (`alt="Pacta Logo"`) | inline `<Img>` in `templates/reset-password.tsx` | `email/template-components/template-branding-logo.tsx` |
| 012 / 015 / 017 / 032 (admin sections) | inline in `admin+/site-settings.tsx` (route is now a thin shell) | `apps/remix/app/components/general/admin-{signup-gating,captcha,webhook,security-headers}-section.tsx` |
| 037 (`created` on signature type) | duplicate `FieldToRender` in `render-field.ts` | single definition in `universal/field-renderer/field-renderer.ts` |
| 041 (PRO claim + trial) | `claim: internalClaims[INTERNAL_CLAIM_ID.PRO]` | `getSubscriptionClaim(INTERNAL_CLAIM_ID.PRO)` — upstream slimmed `internalClaims` to `Pick<'id'\|'name'>`, so flags/quotas now come from the DB row |
| 070 | `070-ship-mca-sources.patch` | **APPLIED 2026-09-07** | LOW | Adds one `COPY` to `docker/Dockerfile`'s runner stage for `packages/bizrethink/mca/sources/` (764K of vendored statutes). `verifyProvenance` re-reads and re-hashes those files at request time, so a verification date without the bytes means nothing. Without this, `/admin/mca` reports SOURCE MISSING for all eleven states in production while being green locally. Same file as overlays 003 and 031. | **Renumbered 069→070 on 2026-09-07:** two sessions independently claimed 069 the same day (this and `069-leases-nav-entry.patch`). Different files, no functional conflict — but overlay numbers are how `UPSTREAM.md` says what to re-apply after a merge, and a duplicate makes that reference ambiguous at exactly the moment it is needed. The later arrival yields. **Regenerated 2026-09-07**: the in-file comment still said "overlay 069" after the renumber, and the trailing context moved when upstream narrowed the runner's prisma generate to `--generator client`.

Overlay 047's `NAV_GROUPS` also gained upstream's two new admin routes (Email Transports,
Organisation Stats) — re-check that array whenever upstream adds an admin page.

**Patch files for 013 / 047 / 052 / 060 / 061 still describe the pre-sync layout and need
regenerating before they can be re-applied mechanically.**

## Production deploy step (one-shot)

After applying 001+002 to a fresh deployment, also run the BIZRETHINK `SubscriptionClaim` INSERT documented in `packages/bizrethink/README.md`. Org creation works without it (no FK constraint), but admin UIs that list tiers won't see BIZRETHINK until the row exists.

## Pending evaluation (queued, not yet patched)

These ideas come from the day-1 spike or feature backlog. Convert to patches as needed.

| Idea | Trigger | Likely complexity |
|---|---|---|
| Override default mailer with Postmark adapter | Day 4–7 BizRethink features | Low — single import swap, possibly via env var |
| Skip deploying `apps/openpage-api` | Day 1 spike if it complicates Docker build | Trivial — docker-compose change |
| Custom theme injection per team | Day 4–7 BizRethink features | Medium — depends on Documenso's theme architecture |
| Add HIPAA / allowLegacyEnvelopes flags to BIZRETHINK tier | If we expand to healthcare or need legacy envelopes | Trivial — two lines in the 001 patch body |

## Convention reminder

Every patch in this directory must:

1. Have a one-paragraph header at the top:
   ```
   # Why: <rationale>
   # Why-not-additive: <why this can't be a new file in packages/bizrethink/>
   # Upstream-merge-fragility: low | medium | high
   ```
2. Be referenced in this README's "Active patches" table
3. Touch as few lines as possible
4. Be applied via `scripts/apply-overlays.sh` (TBD — for now, manually with `git apply`)

If you're tempted to add a multi-hundred-line patch, **stop and ask whether the change can live in `packages/bizrethink/` instead**. Big patches are a smell.
