legacy-peer-deps = true
# MODIFIED for BizRethink: prefer-dedupe MUST stay false. With it true, npm
# drops 9 tailwindcss@3.4.19 CJS transitives and the apps/remix Coolify build
# fails with "Cannot find module 'dlv'" / 'postcss-nested'. See commit cb5690672.
prefer-dedupe = false
# min-release-age = 7
