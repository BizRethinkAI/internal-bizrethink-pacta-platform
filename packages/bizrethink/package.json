{
  "name": "@bizrethink/customizations",
  "version": "0.1.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "license": "UNLICENSED",
  "files": [
    "server-only/",
    "ui/",
    "prisma-extensions/",
    "feature-flags.ts",
    "index.ts"
  ],
  "scripts": {
    "test": "vitest run",
    "typecheck:lease": "tsc -p tsconfig.typecheck.json",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@documenso/email": "*",
    "@documenso/lib": "*",
    "@documenso/prisma": "*",
    "@documenso/trpc": "*",
    "@noble/ciphers": "0.6.0",
    "@react-pdf/renderer": "^4.9.0"
  },
  "devDependencies": {
    "@documenso/tsconfig": "*",
    "@vitest/coverage-v8": "^4.1.6",
    "vitest": "^4.1.6"
  }
}
