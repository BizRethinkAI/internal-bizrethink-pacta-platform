# sync/2026-09-07 — upstream 2.16.0 → 2.17.0 (36 commits)

**PR:** #TBD. Branch `sync/2026-09-08` (the name `sync/2026-09-07` was already
taken on the remote by the weekly automation -- see below). Merges
`documenso/main` @ `2cac63a00`, then `main` @ `9b5d66e35`.

Base for the next sync: this merge's `git merge-base` must be `2cac63a00`.
Merge it with `--merge`, never squash — squashing breaks upstream ancestry
(see STATE.md, "pacta main allows merge commits").

## Why this one was small and still not routine

Thirty-six commits, against 142 last time. But the small number hides a React 18
→ 19 migration (#3107), a Node 22 → 24 base image (#3332), a libpdf upgrade
(#3323), and a rewrite of certificate status that lands directly on the
signing overlay. Commit count is not a proxy for risk here.

## What was resolved by hand, and why

**41 conflicted paths.** The interesting ones:

| Path | Resolution |
|---|---|
| 10 × `.github/workflows/*` | modify/delete → re-deleted (overlay 006) |
| `.npmrc` | kept our `prefer-dedupe = false`; took upstream's `min-release-age = 7` |
| `embed+/_v0+/_layout.tsx` | kept overlay 014's async SSO getters, ADDED upstream's new `useAnalytics`/`useEffect` imports |
| `docker/Dockerfile` | see below |
| `packages/signing/index.ts` + `transports/local.ts` | see below |
| `render-audit-logs.ts` | took upstream's `@documenso/skia-canvas` imports; overlay 038's body untouched |
| 11 × `translations/*/web.po` | entry-granular 3-way merge; see below |
| `package-lock.json` | took upstream's wholesale, then `npm install` (never hand-merged) |

### The signing overlay had to grow an argument

Upstream #3309 rewrote `getCertificateStatus` to inspect the *actual* signer
rather than stat a file, and to call `createLocalSigner({ buildChain: false })`
so a health check does not hit the network for AIA intermediates. Our overlay 011
version of `createLocalSigner` took no arguments.

It now accepts upstream's `{ buildChain }` option and still resolves cert and
passphrase from `BizrethinkInstanceSigningConfig` before falling back to env.
This is failure mode #1 in `UPSTREAM.md`, and the fix is the one that file
prescribes: extend our overlay to accept the new shape, do not revert upstream.

**A side effect worth knowing:** because cert-status now inspects the signer this
function returns, `/api/health` and `/api/certificate-status` report on the cert
we actually sign with — the DB-sourced one included. Before, they only ever
looked at env. That is an improvement, and it is also a behaviour change on a
cryptographically load-bearing surface, so it is called out here rather than
buried in the diff.

Transport selection also now defers to upstream's `NEXT_PRIVATE_SIGNING_TRANSPORT()`
for its env fallback, because #3309 gave that constant the same `|| 'local'`
default our overlay used to spell out. One less divergence.

### We did NOT follow upstream's build heap raise

Upstream #3332 sets `NODE_OPTIONS=--max-old-space-size=8192` in the installer
stage. **Overlay 003 deliberately keeps 4096.** The Coolify host that builds this
image has 8GB of RAM *total*; telling V8 it may grow the heap to 8GB leaves
nothing for the OS and re-creates the silent build OOM this repo already paid
for on a 4GB VPS. Build peaks around 4GB.

If the Docker build ever dies on heap exhaustion, raise it incrementally. Do not
jump to 8192 on an 8GB box. The reasoning is in the Dockerfile, next to the line.

Also taken from upstream in that file: `npx turbo` instead of bare `turbo`, and
`prisma generate --generator client` in the runner stage (upstream narrowed it to
the client generator and added a cache clean; the kysely/json/zod generators only
produce build-time types, so the runner does not need them).

### The translation catalogues needed an entry-aware merge

A plain text merge of `web.po` splits entries down the middle: it left a `msgid`
with no following `msgstr`, which is a broken catalogue. A blind union merge is
worse — for the entries where upstream only changed the *value* it produces two
`msgstr` lines for one `msgid`.

These are generated, append-mostly, msgid-keyed files, so they were merged at
**entry granularity**: each entry folded onto one line with a sentinel, run
through `git merge-file`, then resolved per entry against the merge base rather
than per hunk. Per-hunk was tried first and was wrong — git groups adjacent
entries into one hunk, so "take ours" there silently discards upstream-only
changes to the neighbours.

Result: 5 disjoint insertions per locale kept from both sides, 59 (en) + 54 (pl)
entries taken from upstream where only upstream changed them, and exactly **five
entries genuinely changed on both sides** — all five upstream's British → US
spelling fix (#3329) on strings we have no rebrand stake in, so upstream won.
`Pacta` occurrence counts are identical before and after in every locale, and no
new duplicate `msgid` was introduced.

**`fr/web.po` is corrupt upstream and it is not ours to fix.** It contains a NUL
byte and a committed conflict artifact — `msgstr "<<<<<<< Updated upstream======="`
for msgid `"Attachment removed successfully."` — byte-identical in the merge
base, our `main`, and `upstream/main`. The NUL is why git treats that file as
binary and refused to text-merge it at all; it was merged explicitly instead.
Worth reporting upstream.

## What the sync did NOT break

The three surfaces most at risk were checked directly rather than assumed:

- **`patches/@react-pdf+layout+5.2.0.patch` still applies.** `@react-pdf/renderer@^4.9.0`
  resolves to 4.9.0 (the latest published) and its `@react-pdf/layout@^5.2.0` to
  5.2.0 — the exact pinned version. `npx patch-package` reports both patches ✔,
  and `react-pdf-lineheight-compounding.test.ts` passes. This was the single
  highest-risk item in the sync and it is clean, not lucky: 5.2.0 is still the
  newest `@react-pdf/layout` on npm.
- **Overlay 068 is intact** — upstream did not touch `packages/prisma/seed/documents.ts`,
  and the terminal `.otherwise(async () => null)` is still on the chain.
- **Overlays 047 and 069 are intact** — upstream touched neither the admin
  `_layout.tsx` nor `app-nav-desktop/mobile.tsx` this cycle.
- **Every overlay-066 `testIgnore` target still exists**, so no E2E exclusion has
  silently gone dead.
- **No `warp-*` runner label reintroduced**, so `workflow-runners.test.ts` holds.

## `patches/@ai-sdk+google-vertex+3.0.81.patch` was deleted, correctly

`UPSTREAM.md` listed it as one of "two dependencies this fork patches". It was
never ours: upstream added it in #2271 and deleted it in #3225 when they bumped
`@ai-sdk/google-vertex` to 5.0.48. The merge removed it and added upstream's new
`@radix-ui+react-menu+2.1.24.patch`. `UPSTREAM.md`'s table now records ownership
per row, because a patch you believe is yours is a patch you will defend by
mistake.

## A tool that lies: the overlay reverse-apply check

`git apply --check --reverse overlays/*.patch` looks like a drift detector and is
not one — **38 of 46 patches fail it on pre-merge `main`.** The patches are
historical records generated at different times, not re-appliable artifacts.

Only the *delta* means anything. After this merge exactly two newly fail —
`011a-instance-signing-config` and `070-ship-mca-sources` — and both are files
resolved by hand above. Both patches were regenerated against the merged tree.

## Still open

- The E2E gate is the verdict, and it runs on the PR. A `cancelled` E2E run is
  **not** a pass (this repo has been burned twice); re-run it.
- Coolify auto-deploy is off. After a human merges, the deploy must be triggered,
  and the post-deploy E2E is the third gate.

## Second merge: main @ 9b5d66e35

Seven MCA pull requests (#126, #134-#141) landed while this branch was held, so
`main` was merged in a second time. What that cost:

- **`docker/Dockerfile`** conflicted. `4e884842e` added a `COPY` for
  `mca/clauses/source-documents` immediately above the `prisma generate` line
  this sync rewrote to upstream's `--generator client` form. Both wanted; kept
  both.
- **All eleven `web.po` files** conflicted, as expected. Resolved by taking
  this branch's version (which carries upstream's translation updates that main
  does not have) and then **regenerating by extraction** rather than merging.
  All twelve locales now agree at 3433 msgids, `Pacta` counts match main
  exactly, and the twelve MCA `#:` references are present.
- **`packages/prisma/schema.prisma` auto-merged, and was verified rather than
  trusted.** #140 added a `findings` relation to an *existing* model and
  `prisma format` churned whitespace inside the managed block, which is the
  shape that merges wrongly while looking right. All 19 models from
  `additions.prisma` are present, the relation field survived, no markers.

**The branch name is `sync/2026-09-08`, not `-07`.** `refs/heads/sync/2026-09-07`
already exists on the remote at `68d3b3469`, left by the weekly
`upstream-sync.yml` automation. So do four others: `sync/2026-08-17`,
`sync/2026-08-24`, `sync/2026-08-31`, `sync/manual-2026-08-13`. All five are
stale -- already ancestors of `main`, and their merge-base with upstream is the
*old* `617f8cc2`. **They are worth deleting, in their own change, not this one.**

## npm now gates install scripts

The sync moves `packageManager` from `npm@11.11.0` to `npm@11.19.1`, and CI runs
`corepack enable npm`, so CI starts gating too: `18 packages have install
scripts not yet covered by allowScripts`. Nothing is configured to allow them.

Nothing broke -- the affected packages ship prebuilt binaries through optional
dependencies. Playwright specifically is safe for a reason worth writing down:
`.github/actions/playwright-install` runs an explicit `npx playwright install`,
so browsers never depended on `@playwright/browser-chromium`'s install script.

**This is a live risk for the next dependency that needs its install script**,
and it will present as a missing binary at runtime rather than as a failed
install. If that happens, configure `allowScripts` rather than pinning npm back.

## `overlays/EXCEPTIONS.md` meant two things

PR #141 created this file for **generated output**, with the test "whether a
human wrote the change or a tool did". `UPSTREAM.md` has pointed at the same
path since 2026-04-29 for something else entirely: **sanctioned one-line direct
edits** under *"When to break the rules"*. Both are legitimate and the tests are
opposite, so the file now has two explicitly separated sections.

`.npmrc`'s `prefer-dedupe = false` is recorded in section 2 -- the exception
UPSTREAM.md has required be written down for four and a half months, during
which the file did not exist. The entry says loudly that only that one line is
ours and that `min-release-age = 7` is upstream's, because the failure mode is a
reader "restoring" the file and breaking the Coolify build in a way CI does not
reproduce.

## React 19 removed the global `JSX` namespace, and it cost one line

The local build gate earned its place on this sync. `npx turbo run build` failed
with exactly one error:

```
app/components/general/pacta-signup-hero.tsx(34,17): error TS2503: Cannot find namespace 'JSX'
```

React 19 (#3107) removed the global `JSX` namespace `@types/react` used to
declare. `JSX.Element` now resolves only as `React.JSX.Element`, or through an
explicit `import type { JSX } from 'react'`. The file is fork-owned — overlay
054's Pacta signup hero — so nothing upstream would ever have fixed it.

**The error message names neither React nor a version.** "Cannot find namespace
'JSX'" reads like a missing import in a file whose imports are fine, which is
how a one-token fix turns into an afternoon.

Fixed with `import type { JSX } from 'react'`. Guarded by
`packages/bizrethink/regression-tests/no-global-jsx-namespace.test.ts`, written
red first: it fails on a bare `JSX.` in a fork-owned file and passes on
`React.JSX.` or an explicit import.

A grep rather than a behavioural test, deliberately. The defect is a type that
does not resolve, so only `tsc` can observe it and `tsc` already runs in CI.
What `tsc` cannot do is explain *why* to whoever writes `JSX.Element` out of
habit in six months.

**`packages/ui/primitives/recipient-role-icons.tsx` has the same bare usage and
was deliberately left alone.** It is upstream's, it does not fail today, and
"fixing" it would mean an overlay against a file upstream will keep editing —
carrying cost for no benefit. The guard is scoped to fork-owned paths for that
reason.

**Worth noting what the cheap gates did not catch.** The two vitest suites, the
bizrethink `tsc`, biome and lingui compile were all green with this error
present. Only the full `apps/remix` typecheck sees it — which is the gate that
could not run until Colima was resized from 2GB to 8GB, and the reason
`UPSTREAM.md` calls it non-negotiable.
